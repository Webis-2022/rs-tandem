# Дата: 2026-03-01

**Что было сделано:**

- Созданы необходимые таблицы в базе данных
- Написана edge функция которая проверяет наличие батча вопросов в БД по теме. Если их нет, то запускает генерацию вопросов используя LLM
- Написание функций для взаимодействия с БД
- Сделано ревью кода для PR feature/routing layout auth init

**Проблемы:**

1. Для таблицы game_results забыл создать полис RLS. Из за этого была проблема с сохранением данных в таблицу

2. При создании функции saveProgress был нарушенен принцип разделения ответственности. Я смешал в функции

- UI слой (DOM)

- бизнес-логику

- работу с базой данных (Supabase)

3. Остается очень мало времени на написание кода приложения, в связи с большим объемом задач связанных с координацией работы команды, а также с затратами времени на правильное оформление Гитхаб репозитория.

**Решения (или попытки):**

1. Обработал ошибку возвращаемую функцией getQuestions и тогда смог "докопаться" до сути проблемы.
2. В ходе ревью проблема с функцией saveProgress была выявлена. И впоследствии внесено исправление в виде передачи необходимых данных в функцию через параметры.

**Мысли / Планы:**

Создание страницы Practice и имплементация логики игры.

**Затраченное время:** 20 часов
