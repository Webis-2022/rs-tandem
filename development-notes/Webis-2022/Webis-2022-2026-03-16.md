# Дата: 2026-03-16

**Что было сделано:**

- Реализована супер игра
- Установлены тулзы и настроена конфигурация для unit тестирования
- Написано несколько юнит тестов
- Сделан рефакторинг кода

**Проблемы:**

1. Изначально practice card была реализована таким образом, что при переходе к следующему вопросу происходил полный ререндер контейнера practice card. Такой подход работал, однако с точки зрения пользовательского опыта это не является оптимальным решением, поскольку перерисовывается весь компонент, включая элементы, которые фактически не изменяются.

Я решил изменить логику работы компонента. Теперь при переходе к следующему вопросу должен обновляться только необходимый контент: текст вопроса, варианты ответов и текущее количество баллов. Остальная структура компонента при этом остаётся неизменной.

В течение сегодняшнего дня я занимался рефакторингом существующей логики и переписыванием связанных функций, чтобы реализовать такой подход. В процессе возникли сложности, связанные с изменением текущей архитектуры и зависимостей между функциями. Поэтому часть времени была потрачена на поиск решения и адаптацию существующего кода под новую логику.

**Решения (или попытки):**

1. На данный момент я нахожусь на этапе реализации обновлённой логики обновления контента внутри practice card.

**Мысли / Планы:**

Завершить рефакторинг и реализовать обновлённую логику работы practice card.

Начать разработку Dashboard.

**Затраченное время:**

20 часов
