# Дата: 2026-03-11

**Что было сделано:**

- Закончена реализация логики игры
- Реализован функционал подсказок
- Сделано ревью кода PR#51
- Переписан prompt для LLM

**Проблемы:**

1. При ревью моей ветки implementGameLogic что в игре имеется баг, позволяющий многократное нажатие кнопки Check

2. Также был обнаружен баг со значением раунда.

3. Edge function при генерации вопросов с помощью LLM возвращала на клиент savedData, из за этого сервер падал с ошибкой 502.

**Решения (или попытки):**

1. Добавил в слушатель { once: true }

2. Исправлено значение раунда в стейте

3. Добавлен код

   const questionsBatch = savedData[0].questions_batch.filter(
   (item) => item.level === difficulty
   );

**Мысли / Планы:**

Приступить к реализации супер игры

**Затраченное время:**

12 часов
